<UserControl
    x:Class="Conflux.UI.Controls.EventItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Conflux.UI.Converters" 
    mc:Ignorable="d"
    Background="Transparent">

    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converters:BoolToColorConverter x:Key="VisitedEventColorConverter"/>
        <converters:DateSentenceConverter x:Key="DateSentenceConverter"/>
        <converters:DateToDayConverter x:Key="DateToDayConverter"/>
        <converters:DateToMonthConverter x:Key="DateToMonthConverter"/>
    </UserControl.Resources>

    <Grid Margin="0, 10">
        <Grid.Transitions>
            <TransitionCollection>
                <EntranceThemeTransition 
                    FromHorizontalOffset="0"
                    FromVerticalOffset="20"/>
            </TransitionCollection>
        </Grid.Transitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="55"/>
            <ColumnDefinition Width="15"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" 
              HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="{Binding StartTime, Converter={StaticResource DateToDayConverter}}" Foreground="Black"
                       FontSize="20" HorizontalAlignment="Center"/>
            <TextBlock Grid.Row="1" Text="{Binding StartTime, Converter={StaticResource DateToMonthConverter}}" Foreground="Teal"
                       FontSize="20" HorizontalAlignment="Center"/>
        </Grid>
        <Grid Grid.Column="1" 
              Margin="3, 5, 10, 5" Background="Goldenrod"/>
        <Grid Grid.Column="2"
              VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="{Binding Title}" Foreground="{Binding Visited, Converter={StaticResource VisitedEventColorConverter}}"
                       FontSize="26" TextWrapping="Wrap" TextTrimming="CharacterEllipsis"
                       LineHeight="26" MaxLines="3"/>
            <TextBlock Grid.Row="1" Text="{Binding Location.Name}" Foreground="Black"
                       TextWrapping="Wrap" TextTrimming="CharacterEllipsis"
                       LineHeight="15" MaxLines="2"/>
            <TextBlock Grid.Row="2" Text="{Binding EndTime, Converter={StaticResource DateSentenceConverter}, ConverterParameter='End'}"
                       Foreground="{StaticResource PhoneMidBrush}" LineHeight="15"
                       Visibility="{Binding EndTime, Converter={StaticResource NullToVisibilityConverter}}"/>
            
        </Grid>
    </Grid>

    <!--<Grid Margin="0, 10">
       <Grid.ChildrenTransitions>
            <TransitionCollection>
                <AddDeleteThemeTransition />
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="0" Grid.Column="0" Grid.RowSpan="4" Margin="0, 5, 0, 3"
                                              Background="{Binding Visited, Converter={StaticResource VisitedEventColorConverter}}">
        </Grid>
        <TextBlock Grid.Row="0" Grid.Column="1" Margin="10, 0, 5, 2"
                   TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxLines="3"
                   FontSize="22" LineStackingStrategy="MaxHeight"
                   Text="{Binding Title}" 
                   Foreground="Teal"
                   ContinuumNavigationTransitionInfo.IsExitElement="True"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Margin="10, 2, 5, 5"
                   Visibility="{Binding StartTime, Converter={StaticResource NullToVisibilityConverter}}"
                   Text="{Binding StartTime, Converter={StaticResource DateSentenceConverter}, ConverterParameter='Start'}"
                   Foreground="Black"/>
        <TextBlock Grid.Row="2" Grid.Column="1" Margin="10, 2, 5, 5"
                   Visibility="{Binding EndTime, Converter={StaticResource NullToVisibilityConverter}}"
                   Text="{Binding EndTime, Converter={StaticResource DateSentenceConverter}, ConverterParameter='End'}"
                   Foreground="Black"/>
        <TextBlock Grid.Row="3" Grid.Column="1" Margin="10, 0, 5, 2"
                   Visibility="{Binding Location, Converter={StaticResource NullToVisibilityConverter}}"
                   TextWrapping="Wrap" TextTrimming="CharacterEllipsis"
                   FontSize="16" Text="{Binding Location.Name}" Foreground="Teal"
                   MaxLines="2"/>
    </Grid>-->
</UserControl>
